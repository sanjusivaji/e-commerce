
<body>
  <div class="container mt-4">
    <h2 class="mb-4">Payment Summary</h2>

    <!-- Address Box -->
    <div class="card mb-4 p-3">
      <h4 class="mb-2">Delivering to:</h4>
      <p class="mb-0">
        <strong>{{userAddress.fullName}}</strong><br>
        {{userAddress.houseNo}}, {{userAddress.street}},<br>
        {{userAddress.city}}, {{userAddress.state}} - {{userAddress.pincode}}
      </p>
    </div>

    <!-- Delivery Date Box -->
    <div class="card mb-4 p-3">
      <h4 class="mb-2">Expected Delivery:</h4>
      <h3><strong style="color: rgb(155, 236, 74);">{{deliveryDate}}</strong></h3>
    </div>

    <!-- Cart Items Box -->
    <div class="card mb-4 p-3">
      <h4 class="mb-3">Cart Items:</h4>
      <ul class="list-unstyled">
        {{#each products}}
        <li class="d-flex align-items-center mb-3">
          <img src="/{{this.image}}" alt="{{this.name}}" style="width: 80px; height: auto; border-radius: 10px; margin-right: 15px;">
          <div>
            <strong>{{this.name}}</strong><br>
            Qty: {{this.quantity}}<br>
            ₹{{this.decreasedPrice}}
          </div>
        </li>
        {{/each}}
      </ul>

      <hr>
      <div class="d-flex justify-content-between">
        <span>Subtotal:</span>
        <strong>₹{{subTotal}}</strong>
      </div>
      <div class="d-flex justify-content-between">
        <span>Delivery Charge:</span>
        <strong>₹{{shippingCost}}</strong>
      </div>
      <div class="d-flex justify-content-between">
        <span>Promotion Discount:</span>
        <strong style="color: green;">-₹{{promotionDiscount}}</strong>
      </div>
      <hr>
      <div class="d-flex justify-content-between">
        <h5><strong style="color: rgb(185, 9, 3);">Total:</strong></h5>
        <h5><strong style="color: rgb(185, 9, 3);">₹{{grandTotal}}</strong></h5>
      </div>
    </div>

    <!-- Confirm Order Form -->
    <form id="confirmOrderForm" action="/cash-on-delivery/place-order" method="POST">
      <button id="confirmOrderBtn" type="submit" class="btn btn-success btn-lg">Confirm Order</button>
    </form>
  </div>

  <!-- Overlay -->
  <div id="overlay" style="display: none; position: fixed; top:0; left:0; width:100%; height:100%; background-color: rgba(255,255,255,0.8); z-index: 9999; text-align: center; padding-top: 200px;">
    <h3 class="light-color">Please wait while we confirm your order<span style="color: rgb(243, 57, 40);" class="dot-animate"></span></h3>
  </div>


  <style>
{{!-- For dot animation after text --}}
    .dot-animate::after {
      content: '';
      display: inline-block;
      animation: dots 1.2s steps(3, end) infinite;
      width: 1em;
      text-align: left;
    }

    @keyframes dots {
      0%   { content: ''; }
      33%  { content: '.'; }
      66%  { content: '..'; }
      100% { content: '...'; }
    }
  </style>


  <script>
   // For display the text and dot when click the button 
    document.getElementById('confirmOrderForm').addEventListener('submit', function () {
      document.getElementById('overlay').style.display = 'block';
    });
  </script>
</body>
